---
// layout import
import BaseLayout from "../../layouts/BaseLayout.astro";

// component import
import CardLong from "../../components/ui/CardLong.astro";

import { getCollection } from "astro:content";

let writings = await getCollection("writings", ({ data }) => {
    if (import.meta.env.DEV) {
        return true;
    }
    return data.draft !== true;
});

writings = writings.sort(
    (a, b) => b.data.updatedDate.valueOf() - a.data.updatedDate.valueOf()
);

const {
    title = "Writings",
    description = "", // TODO: update desc
} = Astro.props;
---

<BaseLayout title={title} description={description}>
    <section class="container-grid">
        <section class="container-side"></section>

        <section class="container-main">
            <article>
                <h1>
                    {title}<span class="text-red-800">.</span>
                </h1>
                <span class="font-medium text-neutral-500 italic">
                    {description}
                </span>
            </article>

            <div class="@container mt-8 flex flex-col gap-y-4">
                {
                    writings.map((writing) => (
                        <CardLong post={writing} collection="writings" />
                    ))
                }
            </div>
        </section>

        <section class="container-side"></section>
    </section>
</BaseLayout>
